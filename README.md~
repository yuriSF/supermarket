Supermarket Optimization
==============

**Algorithm**

**How to run**
Use Python to run the __init__.py file.
Mac/Linux:
In the terminal window, navigate to the application's directory and type python
__init__.py. No command line arguments are needed. The sigma value of 2 is hard
coded into program as a parameter to the count_overlaps() function. The database
file is included in the directory. The program outputs the results to the file
specified by the user as a command line argument.

To run the program type in the terminal:
python __init__.py output.txt

 in the format:

XXXX

Sample output is provided in the file out.txt
